#include "pch.h"
#include "VulkangltfModel.h"
/*
void vkglTF::Texture::updateDescriptor()
{
    descriptor.sampler = imageSampler;
    descriptor.imageView = imageView;
    descriptor.imageLayout = imageLayout;
}

void vkglTF::Texture::destroy()
{
    if (core)
    {
        vkDestroyImageView(core->GetDevice(), imageView, nullptr);
        vkDestroyImage(core->GetDevice(), image, nullptr);
        vkFreeMemory(core->GetDevice(), imageMemory, nullptr);
        vkDestroySampler(core->GetDevice(), imageSampler, nullptr);
    }
}

void vkglTF::Texture::fromglTfImage(tinygltf::Image& gltfimage, std::string path, VulkanCore* core, VkQueue copyQueue)
{
}
*/